| SYNTAX TEST reindent-unchanged "Packages/Twig/Syntaxes/HTML/HTML (Twig).sublime-syntax"

{% import "template1.html" %}
{% import "template2.html" %}
{% from "template3.html" import macro3 %}
{% from "template4.html" import macro4 %}

<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <title>Page Title Here</title>
</head>
<body>

    <div class="main">
        <span class="something">Blah</span>
        <span class="something">
            Foobar
        </span>
    </div>

    <script type="text/javascript" src="/some_script.js"></script>

    {% block content %}
        <script type="text/json">
            {
                "dict": {
                    "list": [
                        "value"
                    ]
                }
            }
        </script>
    {% endblock content %}

    <!-- Curly braces -->
    foo {
        bar { <!-- comment -->
            baz
        }
    }
    text
    { <p></p> }
    text
    {{template}}
    text

    {% embed "teasers_skeleton.twig" %}
        {{ "string" }}
    {% endembed %}

    {% if block("footer") is defined %}
        {% if foo = 1 %}
            {% if foo = 1 %}
                foo
            {% elseif me = 2 %}
                {% if foo = 1 %}
                    foo
                {% elseif me = 2 %}
                    bar
                {% else %}
                    {% if foo = 1 %}
                        foo
                    {% elseif me = 2 %}
                        bar
                    {% else %}
                        baz
                    {% endif %}
                {% endif %}
            {% else %}
                baz
            {% endif %}
        {% elseif me = 2 %}
            bar
        {% else %}
            baz
        {% endif %}
    {% elseif true %}
        {{ "string" }}
    {% else %}
        {{ "string" }}
    {% endif %}

</body>
</html>
